1.  **Analyze the Request**:
    *   Reference identifier: `p4v2-shadow-gate-fixed`.
    *   Corpus: A long list of 229 sections (0 to 228).
    *   Each section follows a strict template: "Section N. In practice [topic]; the resulting behaviour depends on configuration [X] and on the observed interaction between stage [A] and stage [B] of the surrounding system."
    *   Task: "Briefly summarise the corpus above."

2.  **Analyze the Corpus Structure**:
    *   There are 8 distinct topics that repeat cyclically:
        0. distributed inference schedulers allocate pipeline stages across nodes
        1. thunderbolt fabrics expose queue pairs with distinct completion semantics
        2. mixture of experts routing selects a sparse subset of feedforward blocks
        3. key value caches grow linearly with sequence length during autoregressive decode
        4. quantised weight formats trade