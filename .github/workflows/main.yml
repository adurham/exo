name: Deploy to Home Cluster

on:
  push:
    branches:
      - local_mac_cluster
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, exo-deployer]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy to All Nodes
        run: |
          # --- CONFIGURATION ---
          NODES=("192.168.86.100" "192.168.86.101" "192.168.86.103")
          SSH_USER="adam.durham"
          
          for NODE in "${NODES[@]}"; do
            echo "------------------------------------------------"
            echo "📡 Connecting to $NODE..."
            ssh -o StrictHostKeyChecking=no "$SSH_USER@$NODE" 'bash -s' < scripts/deploy_node.sh
          done

      # --- NEW STEP 1: Verify UI is Up ---
      # - name: 🏥 Health Check (Head Node UI)
      #   run: |
      #     HEAD_IP="192.168.86.100"
      #     SSH_USER="adam.durham"
      #     PORT="52415"
      #     MAX_RETRIES=60  # Increased to 5 minutes
          
      #     echo "⏳ Waiting for Exo UI to respond on $HEAD_IP via SSH..."
          
      #     count=0
      #     # We SSH into the machine and run curl locally there to avoid firewall/routing issues
      #     until ssh -o StrictHostKeyChecking=no "$SSH_USER@$HEAD_IP" "curl -s --head --request GET http://localhost:$PORT | grep '200 OK'" > /dev/null; do
      #       if [ $count -eq $MAX_RETRIES ]; then
      #         echo "❌ Timeout: Web UI never came online."
      #         exit 1
      #       fi
      #       printf "."
      #       sleep 5
      #       count=$((count+1))
      #     done
          
      #     echo ""
      #     echo "✅ Head Node UI is Online!"

      # --- NEW STEP 2: Verify All Nodes Connected ---
      # - name: 🕵️ Verify Cluster Topology (Api Check)
      #   run: |
      #     HEAD_IP="192.168.86.100"
      #     SSH_USER="adam.durham"
      #     PORT="52415"
      #     EXPECTED_NODES=3
          
      #     echo "🔍 Querying API for connected devices..."
          
      #     # Fetch JSON via SSH
      #     API_RESPONSE=$(ssh -o StrictHostKeyChecking=no "$SSH_USER@$HEAD_IP" "curl -s http://localhost:$PORT/state")
          
      #     # Debug: Print it so you can see what happened if it fails
      #     echo "Raw API Response: $API_RESPONSE"
          
      #     # Parse the JSON to count the devices list
      #     NODE_COUNT=$(echo "$API_RESPONSE" | python3 -c "import sys, json; print(len(json.load(sys.stdin).get('topology', {}).get('nodes', [])))")
          
      #     echo "📊 Active Nodes Found: $NODE_COUNT"
          
      #     if [ "$NODE_COUNT" -lt "$EXPECTED_NODES" ]; then
      #       echo "⚠️ Warning: Expected $EXPECTED_NODES nodes but only found $NODE_COUNT."
      #       # Uncomment the next line to FAIL the build if a node is missing
      #       # exit 1 
      #     else
      #       echo "✅ Full Cluster is Healthy!"
      #     fi