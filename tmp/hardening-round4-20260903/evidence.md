# Hardening Round 4 — Evidence: full-suite failure split + formatting gate without nix

Run against HEAD `1840c37ad83ddbe50bfc9180a7044f5a6051a473` (branch `main`), macOS, local venv.
Command:
```
EXO_TESTS=1 EXO_DASHBOARD_DIR="$PWD/dashboard/" EXO_RESOURCES_DIR="$PWD/resources" \
  .venv/bin/python -m pytest src -m "not slow" --import-mode=importlib -q
```
Local `mlx` build: `0.32.0.dev20260804+ac73d0c9` (adurham/mlx@`ac73d0c9`), local `mlx_lm`: adurham/mlx-lm@`37260bbd6`.

## Part A: full-suite failure split

**Verbatim final summary line:**
```
8 failed, 1098 passed, 4 skipped, 205 deselected, 1 warning in 60.85s (0:01:00)
```
(1110 collected via 1098+8+4=1110 tests actually run+failed+skipped, 205 deselected by `-m "not slow"` — matches the stated 1110-collected / 0-errors baseline from 1840c37ad.)

**Bucket counts: 0 ENVIRONMENTAL / 8 REAL / 0 UNCLEAR**

Every failure's traceback was traced to its root cause in the source (via `git log -p`/`git blame` on the touched code and, where relevant, the vendored `mlx`/`mlx_lm` submodules) before classification. None of the 8 implicate an mlx symbol/signature delta between the local dev build and CI's nix pin — each is a genuine code/test drift bug that would reproduce identically under CI's pinned mlx.

| # | Test | Bucket | Evidence (verbatim traceback line + root cause) |
|---|------|--------|---------------------------------------------------|
| 1 | `src/exo/master/tests/test_routing_concurrency.py::test_concurrent_text_generation_distributes_across_instances` | REAL | `pydantic_core._pydantic_core.ValidationError: 1 validation error for ModelCard\n  backends\n  Field required [type=missing, ...]`. `ModelCard.backends: list[Backend]` is a required field (added in `bc6661e6a` "Add node backends to model cards"); the test's `_make_model_card()` helper in `test_routing_concurrency.py` never passes it. Test wasn't updated when the schema changed — not mlx-related at all. |
| 2 | `src/exo/master/tests/test_routing_concurrency.py::test_text_generation_does_not_cross_model_boundaries` | REAL | Identical cause/traceback to #1 — same `_make_model_card()` helper, same missing `backends` field. |
| 3 | `src/exo/worker/engines/mlx/tests/test_batch_generate_batched_decode_flag_off_smoke.py::test_exobatchgenerator_flag_off_matches_verified_pre_edit_baseline` | REAL | `assert [2842, 1155, ...] == [1686, 1475, ...]` — token-for-token mismatch against a hardcoded golden-token baseline that the test's own docstring says was captured from "a real ExoBatchGenerator instance" via "the git-worktree A/B run documented in this module's own docstring." Sampling/tokenization output drifted from that frozen baseline; this is a golden-value staleness bug in the test fixture, not an mlx API signature/symbol error — the call path executed to completion and produced *different but valid* tokens. |
| 4 | `src/exo/worker/engines/mlx/tests/test_pp_metaframe.py::test_metaframe_protocol_version_mismatch_raises` | REAL | `AssertionError: assert 4 == 3` — the test hardcodes `_mod.METAFRAME_PROTOCOL_VERSION == 3`, but the constant is `4` in current source (bumped by commit `491d5ea35` "pp: metaframe PHASE_CANCEL frame type (protocol v4)"). Pure test/constant drift; no mlx involvement. |
| 5 | `src/exo/worker/engines/mlx/tests/test_pp_speculation_cache_snapshot.py::test_pooling_cache_with_untouched_buffers_does_not_crash_snapshot` | REAL | `assert (None, None, None, None, None) == (None, None, None)` — `PoolingCache.state` in the vendored `mlx_lm` (`.venv/.../mlx_lm/models/cache.py`) returns a 5-tuple (added an overlap-carry pair alongside the original 3-tuple), but the test still asserts the old 3-tuple shape. Same `mlx_lm` commit (`37260bbd6`) is what CI would also build against per the flake pin, so this reproduces there too — it's a test/library-shape drift, not a version *mismatch* between local and CI. |
| 6 | `src/exo/worker/tests/unittests/test_runner/test_concurrency_admission_gate.py::test_second_task_deferred_until_first_completes` | REAL | `AttributeError: 'HoldingExoBatchGenerator' object has no attribute 'set_prefill_cancel_probe'`. The test's own hand-written fake/mock class (`HoldingExoBatchGenerator`, defined in this same test file) doesn't implement `set_prefill_cancel_probe()`, a method that production `ExoBatchGenerator` (`batch_generate.py:2175`) already has. Test double fell out of sync with the interface it doubles — no mlx code executes here at all. |
| 7 | `src/exo/worker/tests/unittests/test_runner/test_concurrency_admission_gate.py::test_two_concurrent_slots_admits_both_immediately` | REAL | Identical cause/traceback to #6 — same missing method on the same fake class. |
| 8 | `src/exo/worker/tests/unittests/test_runner/test_event_ordering.py::test_events_processed_in_correct_order` | REAL | `TypeError: all_min(): incompatible function arguments... Invoked with types: mlx.core.array, kwargs = { group: ...MockGroup, stream: ... }`. Confirmed via direct repro (`mx.distributed.all_min(mx.array(5), group=FakeGroup())`) that this `TypeError` is a **nanobind interface contract violation**, not a version delta: the C++ binding (`mlx/python/src/distributed.cpp`) requires an actual `mx.distributed.Group` instance for the `group=` kwarg, and this test's hand-rolled `MockGroup` (a plain Python duck-type with `.rank()`/`.size()`) has never satisfied that contract — reproduces identically on any mlx build with this binding signature, local or CI-pinned. The test's fixture was written for an older code path (originally the mock group was the literal int `1`, per `git log`) before `mx_min_int()` (added in `776c123bc`) started routing per-step gating through a real `mx.distributed.all_min()` call; the fake was never upgraded to a real/compatible group stand-in. |

**No environmental failures were found.** The task's prior expectation of "~6 environmental failures from the mlx version mismatch" did not hold up under traceback-level inspection of this run — every failure traces to test/production drift (stale fixtures, missing fields, hardcoded stale constants, out-of-sync mocks) that would reproduce under CI's nix-pinned mlx too, since none of the 8 tracebacks implicate an mlx symbol or call-signature delta between the local dev build and the flake's pin.

## Part B: formatting gate

Per-formatter availability and check-only result (nothing was written; no files were reformatted):

| treefmt formatter | Runnable locally without nix? | Check-only result |
|---|---|---|
| **ruff-format** (excludes `rust/exo_rs/exo_rs.pyi`) | Yes — `uv run ruff format --check .` (ruff 0.14.11); pyproject.toml already `extend-exclude`s `rust/exo_rs/**` | **NOT clean.** 427 files would be reformatted total, but 133 of those are pre-existing untracked scratch files under `tmp/*/` (not evaluated — not part of the repo's tracked source). Restricting to tracked/real code: **294 files** outside `tmp/` would be reformatted, spanning `bench/` (128), the `mlx`/`mlx-lm` git submodules (84 — vendored third-party code, not this repo's own source), `src/` (73 — includes the codebase's own `exo/` package), plus `scripts/`, `tools/`, and a handful of root-level `test_*.py` files (9). |
| **rustfmt** (`config.rust.toolchain` in nix; local: rustfmt 1.9.0 via `cargo`/`rustup`) | Yes — `rustfmt --check --edition 2021` on every `rust/**/*.rs` | **NOT clean.** 6 of 14 `.rs` files under `rust/` have formatting diffs (import-ordering only, e.g. `use zenoh::{Result, Session as ZSession, config::Locator}` → `use zenoh::{config::Locator, Result, Session as ZSession}`): `rust/exo_rs/src/lib.rs`, `rust/exo_rs/src/networking.rs`, `rust/exo_rs/src/pidfile.rs`, `rust/networking/src/discovery.rs`, `rust/networking/src/lib.rs`, `rust/networking/src/swarm.rs`. Caveat: no `rust-toolchain.toml` pins an edition/toolchain in this repo, so `--edition 2021` was assumed to match `Cargo.toml`'s `edition.workspace = true`; the nix-pinned `config.rust.toolchain` version could format slightly differently, though import-grouping rules are stable across recent rustfmt releases. |
| **prettier** (`prettier-svelte` package; scope `*.ts` `*.svelte`) | Yes — dashboard has a local install with `prettier-plugin-svelte` (`dashboard/node_modules/.bin/prettier`, prettier 3.4.2 per `package.json`) | **Clean.** `prettier --plugin=prettier-plugin-svelte --check "src/**/*.ts" "src/**/*.svelte" "*.ts"` (run from `dashboard/`) → "All matched files use Prettier code style!" (The unscoped `**/*.ts`/`**/*.svelte` glob initially flagged 8 files, but all 8 are `.svelte-kit/` build-generated artifacts, gitignored per `.gitignore:29`, not source.) |
| **taplo** (`*.toml`) | **No.** `which taplo` → not found; not installed via Homebrew (`brew list taplo` → no such keg); no vendored/cargo-installed binary found. | Cannot check locally. 159 `.toml` files exist in the tracked tree (excluding submodules/venv/tmp/node_modules) whose formatting is entirely unverified without nix. |
| **shfmt** (shell scripts) | **No.** `which shfmt` → not found. | Cannot check locally. 15 `.sh` files in the tracked tree are unverified. |
| **swift-format** (`*.swift`) | Yes — Xcode Command Line Tools ship one (`xcrun --find swift-format` → `/Library/Developer/CommandLineTools/usr/bin/swift-format`); note this is **not** the same binary/version as `pkgs.swiftPackages.swift-format` that nix would use, so results are indicative, not authoritative. | **NOT clean.** `swift-format lint --strict` on the 25 tracked `*.swift` files (under `app/EXO/`) → 1 diagnostic: `./app/EXO/EXO/EXOApp.swift:262:1: error: [LineLength] line is too long`. |
| **nixpkgs-fmt** (`*.nix`) | **No.** `which nixpkgs-fmt` → not found (requires nix/nixpkgs). | Cannot check locally. 7 `.nix` files in the tree are unverified. |

**Verdict:** The tree is **not** formatting-clean to the extent checkable locally — of the 5 formatters runnable without nix (ruff, rustfmt, prettier, swift-format, and the availability check itself), 3 report real diffs (ruff: 294 non-`tmp` files, rustfmt: 6 files, swift-format: 1 line) and only prettier's `*.ts`/`*.svelte` scope is clean; **taplo (159 `.toml` files), shfmt (15 shell scripts), and nixpkgs-fmt (7 `.nix` files) cannot be verified at all without nix**, so `nix flake check`'s formatting gate carries residual, entirely unverified risk across those three formatters/253 files until it is actually run in an environment with nix installed.
